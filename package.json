{
  "name": "collection-query",
  "version": "0.0.8",
  "description": "A collection processing library.",
  "main": "./collection-query.js",
  "types": "./collection-query.d.ts",
  "scripts": {
    "init": "npm install",
    "build": "npx gulp build",
    "init-test": "npm run build && cd test && npm install && cd ..",
    "test": "npx tsc --project test/tsconfig.json && jest",
    "test-coverage": "npx tsc --project test/tsconfig.json && jest --coverage",
    "pack": "npx gulp pack",
    "publish": "npx gulp pack && npm publish ./package"
  },
  "author": "i-play-lf2",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-typescript": "^6.0.0-alpha.1",
    "jest": "^26.6",
    "typescript": "^4.1.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Iplaylf2/collection-query.git"
  },
  "keywords": [
    "collection",
    "query",
    "typescript",
    "stream"
  ]
}
